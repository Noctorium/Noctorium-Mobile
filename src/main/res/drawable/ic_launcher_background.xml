<?xml version="1.0" encoding="utf-8"?>
<!--
  The field the artwork sits on, and nothing else.

  Black rather than the violet it used to be: the artwork's own background is black, and the two layers
  have to meet without a seam — a launcher mask cuts through both at once, and any difference between
  them would show as a ring around the picture.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:fillColor="#000000" android:pathData="M0,0h108v108h-108z" />
</vector>
